로컬스토리지 
setItem(key, value) - 키, 값 쌍을 보관합니다.
getItem(key) - 키에 해당하는 값을 받아옵니다.
removeItem(key) - 키와 해당하는 값을 받아옵니다.
clear() - 모든 것을 삭제합니다.
key(index) - 인덱스(index)에 해당하는 키를 받아옵니다.
length - 저장된 항목의 개수를 얻습니다.